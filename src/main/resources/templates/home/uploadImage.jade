doctype html
head
	meta(charset="utf-8")
	meta(http-equiv="X-UA-Compatible", content="IE=edge")
	link(rel='stylesheet', href='/css/bootstrap.css', type='text/css', media='screen')
	script(src='/js/bootstrap.js')
	include general-includes.jade
body
	include forms/header.jade
	form(method="post", enctype="multipart/form-data")
		
		input(type="file", name="files", multiple='multiple', accept="image/jpeg,image/png,image/gif")
		input(type="submit", value="Сохранить") Press here to upload the files!
	//- if (path != null && path != false)
	//- 	p Ссылка на последний загруженный файл:
	//- 	input(value="#{path}", readonly)